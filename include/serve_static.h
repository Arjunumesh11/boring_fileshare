#include <string>

namespace serve_static
{
    //int static(std::string folder)
    //@param : folder
    //@return : -1 if not successfull
    int serve_static(std::string folder);

} // namespace serve_static
